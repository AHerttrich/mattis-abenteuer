---
description: Start the Vite dev server for the voxel game
---
// turbo-all

1. Start the Vite development server:
   ```
   npm run dev
   ```

2. The game will be available at `http://localhost:5173` (or the next available port).
   - Press **N** to open the network menu for co-op play.
